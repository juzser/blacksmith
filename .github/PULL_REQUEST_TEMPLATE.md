<!--
Keep this template. Delete the sections that genuinely do not apply, and say
why they do not apply rather than deleting them silently.
Full conventions: CONTRIBUTING.md
-->

## What changed and why

<!-- One paragraph. Lead with what forced the change — the defect, the
     drift, the missing gate — not with the diff. If there is a finding id
     (D-###, P9-##) or an issue number, put it here. -->

## What I actually ran

<!-- Paste the tail of the run, not a claim about it. `bash scripts/check.sh`
     ends in `== PASS ==`; anything that printed SKIP is a step that did NOT
     prove anything, so name it. If you ran only a subset, say which. -->

```
$ bash scripts/check.sh
...
== PASS ==
```

- [ ] `bash scripts/check.sh` is green on this branch
- [ ] `pnpm test:e2e` run, or skipped — if skipped, say why here:

## Screenshots

<!-- Required for anything with a UI surface: desktop and mobile (390px),
     light and dark, at most four per feature. Generated by `pnpm test:e2e`
     into ui/e2e/__screenshots__/ — commit only the ones this change actually
     altered and revert the rest.

     If the change has no UI surface, delete the table and write:
     "No UI surface." -->

| Desktop | Mobile (390px) |
| --- | --- |
| <img src="" width="440"> | <img src="" width="220"> |

## Known findings

<!-- Anything you found and did not fix, with a severity from
     factory/policies/severity.yml:
       S1 stops the line · S2 blocks this PR · S3 batches into a waiver
       question · S4 is logged only.
     "None" is a valid answer, and a much better one than silence. -->

None.

## Generated artifacts

<!-- Tick only what this PR legitimately touches. `factory/policies/lessons.md`
     is written by `smith lessons compile`, and screenshots by the e2e suite —
     neither is hand-editable (CONTRIBUTING.md § Generated files). -->

- [ ] No generated file was hand-edited
- [ ] Any regenerated file was regenerated by its own tool, and the command is
      named above
