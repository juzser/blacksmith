# Make the package.json "engines" field enforced, not advisory.
# Node 20 loads the prebuilt better-sqlite3 binding and then segfaults with no
# message (finding D-47); a hard install-time failure is the cheaper lesson.
engine-strict=true
