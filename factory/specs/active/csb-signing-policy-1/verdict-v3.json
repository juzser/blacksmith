{
  "plan_version": 3,
  "epic_id": "csb-signing-policy-1",
  "integration_sha": "931079e",
  "base_sha": "69f7613",
  "criteria": [
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C1 STRUCTURE AND MAPPING",
      "status": "pass",
      "evidence": "Carried from verdict-v2 (graded at 240588a) and re-anchored on 931079e: `git diff --name-only 240588a..931079e` is exactly .github/workflows/ci.yml, so every Swift/script line below is unmoved. Spot-checked on 931079e with `git show 931079e:<path>`: Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift:25 'public enum PathClass {' :26 'case app' :27 'case claude'; :35-36 'public static let claudeRequirement =' / '\"anchor apple generic and identifier \"com.anthropic.claude-code\" and certificate leaf[subject.OU] = \"Q6L2SF6YDW\"\"' (csreq -r - -t on that literal exited 0 in the v2 dispatch); :44 'public static let selfCertificatePrecondition = \"certificate leaf[subject.CN] exists\"'; :57-59 'public static func classify(_ path: String, appBundlePath: String) -> PathClass?' / 'if path == appBundlePath { return .app }' / 'if URL(fileURLWithPath: path).lastPathComponent == \"claude\" { return .claude }'; `grep -c policyRequirement` on the gate file at 931079e = 0. Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift:36 pins the OU literal; :44-47 assert the four mapping cases (('/tmp/A.app','/tmp/A.app') -> .app, ('/tmp/x/claude','/tmp/A.app') -> .claude, ('/bin/ls','/tmp/A.app') -> nil, ('/tmp/x/claude','/tmp/x/claude') -> .app). Lineage re-measured: 931079e parents = 240588a 3c1dc1b; first-parent chain 931079e <- 240588a <- 2493d04 <- 7d530e5 <- 69f7613."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C2 CLAUDE CLASS REJECTS WHAT THE OLD POLICY ADMITTED",
      "status": "pass",
      "evidence": "Carried from verdict-v2; lines unmoved at 931079e (task-4 touched only the workflow). Spot-checked on 931079e: TrustedApplicationGateTests.swift:143 '@Test func defaultVerifyRejectsLocalIdentitySignedClaudeSpecimen() throws {' and :155 '#expect(TrustedApplicationGate.defaultVerify(specimen.path) == false)'. From v2: :152 codesign ['--force', '--sign', 'ClaudeStatusBar Local Signing', '-i', 'com.anthropic.claude-code']; presence decided at :101-104 by /usr/bin/security find-certificate -c 'ClaudeStatusBar Local Signing' <home>/Library/Keychains/login.keychain-db; the skip branch :145-149 prints, records via withKnownIssue and returns (task-3). :159 defaultVerifyRejectsAdHocSignedClaudeSpecimen signs '--sign -' at :161, asserts false at :164. :171 defaultVerifyRejectsAppleSignedWrongIdentifierClaudeSpecimen copies /bin/ls at :177 and asserts false at :178. Identity PRESENT on this host (read-only find-certificate exit 0 in this dispatch, output discarded). Suite evidence on the final tree: task-3 swift-test.txt and tester-swift-test.txt (bc4a3fc, tree-identical to 240588a for every non-workflow path) and task-4 swift-test.txt at 3c1dc1b (226,410 bytes, 'Test run with 434 tests passed after 3.223 seconds.', 0 'recorded a known issue' lines) plus the grader's own task-4 run ('Test run with 434 tests passed after 3.170 seconds.', grep -c 'recorded a known issue' = 0): the probe ran, no skip fired, the test passed."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C3 CLAUDE CLASS ADMITS THE REAL BINARY",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. TrustedApplicationGateTests.swift:186 defaultVerifyAcceptsRealClaudeBinaryIfPresent; :187 'let homeDir = NSHomeDirectory()' :188 'let candidates = LiveCredentialWriter.claudeBinaryCandidates'; :201 gates on 'TeamIdentifier=Q6L2SF6YDW'; :209-213 skip branch (print + withKnownIssue + return); :215 '#expect(TrustedApplicationGate.defaultVerify(qualifying) == true)'. LiveCredentialWriter.swift:253 claudeBinaryCandidates is read, never edited (the epic diff 69f7613..931079e on that file contains no line naming it). Host measurement (v2 dispatch): codesign -dv ~/.local/bin/claude -> Identifier=com.anthropic.claude-code, TeamIdentifier=Q6L2SF6YDW; 'codesign -v -R=<claudeRequirement>' on it exited 0. Suite logs: 'Test defaultVerifyAcceptsRealClaudeBinaryIfPresent() passed' in task-3 swift-test.txt and tester-swift-test.txt; task-4 swift-test.txt and the grader's task-4 run report 434 passed with 0 known issues, so the Developer-ID branch did not fire on this host."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C4 APP CLASS = 'SIGNED THE WAY THIS PROCESS IS SIGNED'",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Gate: TrustedApplicationGate.swift:148 'public static func verify(_ path: String, appBundlePath: String, selfCode: SecStaticCode?) -> Bool'; :117-128 appClassOutcome: (i) :118 'guard let selfCode else { return .noSelf }', (ii) :120-121 SecStaticCodeCheckValidity(selfCode, validationFlags, precondition) == errSecSuccess else .selfFailsPrecondition, (iii) :124-126 SecCodeCopyDesignatedRequirement == errSecSuccess else .noDesignatedRequirement, (iv) :128 candidateSatisfies(candidatePath, requirement: selfRequirement). Tests: :335 c4And5AppClassIdentitySignedCases; :349 's4 = try appClassSpecimen(sign: \"-\", identifier: \"com.juzser.ClaudeStatusBar\")' (same identifier as S1); (4a) :358 verify(s2,s2,s1Code)==true; (4b) :361 verify(s4,s4,s1Code)==false; (4c) :364 verify(s3,s3,s1Code)==false; (4d) :367 verify(s4,s4,s4Code)==false. (4e) :392 c4eMakeAppScriptSetsNoCustomDesignatedRequirement resolves '../scripts/make-app.sh' (:395) and asserts at :401-402 '!tokens.contains(\"-r\") && !tokens.contains(\"--requirements\")' with text 'make-app.sh:<n> sets a custom designated requirement; the app class relies on the default DR'; scripts/make-app.sh:102 and :104 still read 'codesign --force --options runtime --timestamp --sign ...' and the file has no diff in 69f7613..931079e (`git diff --name-only 69f7613..931079e` lists 11 files, none of them make-app.sh). Both tests passed in the task-3 and task-4 suite logs (434 passed)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C5 NEITHER CLASS, AND NOTHING LOOSENED",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: TrustedApplicationGateTests.swift:223 '#expect(TrustedApplicationGate.defaultVerify(\"/bin/ls\") == false)'. From v2: (5a) :376 'verify(renamed.path, appBundlePath: s1.path, selfCode: s1Code) == false'. (5b) :380 c5bUnsignedDirectoryAppClassWithNilSelfIsRejected, :387 'verify(appDir.path, appBundlePath: appDir.path, selfCode: nil) == false'. (5c) :76 defaultVerifyRejectsAdHocSignedCode and :238 defaultVerifyRejectsUnsignedDirectory survive; the epic diff's removed #expect lines are the three C1/C5d/C5e replacements. (5d) :218 defaultVerifySanityApproveClaudeClassRejectPlainFile with :233 plain-file rejection kept. (5e) the old self-signed-accepted test is gone; :143 is its inverse. (5f) @Test counts re-measured with `git show <sha>:<test file> | grep -c @Test`: 36 at 69f7613, 52 at 240588a, 52 at 931079e; summary 'Test run with 434 tests passed' in task-3 swift-test.txt, tester-swift-test.txt, task-4 swift-test.txt and the task-4 grader run."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C6 THE GATE STAYS SINGLE-FILE AND SUBPROCESS-FREE",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: TrustedApplicationGateTests.swift:806 '@Test func defaultVerifyClosureStaysInFile() throws {' and :817 'let verdict = DefaultVerifySpawnCheck.check(text, label: \"TrustedApplicationGate.swift\")'. From v2: (6a-ii) :813 comment 'defaultVerify is not declared as a func in TrustedApplicationGate.swift'; :815-816 reached.isSuperset(of: ['defaultOutcome', 'copySelfStaticCode']) with 'closure from defaultVerify does not reach: ...'; :1369 'static func reached(_ sourceText: String) -> Set<String>'. (6a-iii) :824 spawnCheckWalkRules: alpha :832/:849 'acquireSelf()', gamma :853 expects 'defaultVerify -> defaultOutcome -> copySelfStaticCode', beta :864 expects 'defaultVerify -> p.spawnHelper', :870 'Self.helper()'; :1325 one 'knownFrameworkSymbols: Set<String> = [' with 10 names; the plan's allowlist loop printed no 'ALLOWLIST NAMES REPO SYMBOL' line (v2 dispatch). (6a-iv) gate file: grep for Process|NSTask|posix_spawn|exec( and for '\"/(usr|bin|sbin|Applications)' both empty; `grep -rlE 'func (defaultOutcome|copySelfStaticCode|classify|verify)\\b' Sources/` prints exactly Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift; :787 defaultVerifySourceContainsNoSpawnTokens kept; 16 fixtures present. (6b) probe (b) :676 and probe (g) :693 have no lines in any diff after task-1. (6c) gate :232 'onReject(path, \"per-path-class policy rejected: (className)\")' with :228-230 'app'/'claude'/'no path class'; test :916-921 filterRejectionReasonNamesPerPathClassNotAPolicyString. Both closure tests passed in the task-3 and task-4 suite logs."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C7 THE SINK NEVER CREATES AN EMPTY ACL",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: `git grep -n 'SecAccessCreate(' 931079e -- Sources` finds exactly AccountCredentialVault.swift:224 and LiveCredentialWriter.swift:152, both 'SecAccessCreate(service as CFString, trustedApps as CFArray, &access)'. From v2: gate filter body unchanged, :220 'guard !paths.isEmpty else { return [] }', :235 'return approved.isEmpty ? nil : approved'. LiveCredentialWriter.swift:113 'makeTrustedApplication: (String) -> SecTrustedApplication? = TrustedApplicationGate.defaultMakeTrustedApplication' (gate :185); :133-134 comment (i); :138 'if !filteredPaths.isEmpty {' :139 compactMap(makeTrustedApplication) :140-149 'guard !trustedApps.isEmpty else { ... return false }' with comment (ii) at :141-143; :151-152 comment (iii) then SecAccessCreate; :165-167 'if let access { newAttributes[kSecAttrAccess as String] = access }'. AccountCredentialVault.swift:189, :208, :212-224, :232-234 mirror it. Tests: LiveCredentialWriterTests.swift:340 performWriteWithEmptyTrustedPathsOmitsAccessKey, :360 performWriteRefusesWhenApprovedPathsYieldNoTrustedApplication (:376-377 updateCalls == 0, addCalls == 0); AccountCredentialVaultTests.swift:316 performRepairWriteWithEmptyTrustedPathsOmitsAccessKey (:326 capturedAttributes?[kSecAttrAccess as String] == nil), :332 performRepairWriteRefusesWhenApprovedPathsYieldNoTrustedApplication (:345-348). 'trustedPaths: []' sites: 14 (8 + 6; 12 pre-existing plus one 7a test per sink), none in any task diff. (7c) met by the gate doc block :191-213 (task-3 C3)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C8 THE COMPOSED REGRESSION",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. LiveCredentialWriterTests.swift:388 performWriteAllRejectedWritesExactlyOneDiagnosticLine, :401 'onReject: TrustedApplicationGate.rejectionHandler(writingTo: logURL)', :407-408 updateCalls == 0 / addCalls == 0, :413 'lines[0].components(separatedBy: <tab>).count == 3'. AccountCredentialVaultTests.swift:353 performRepairWriteAllRejectedWritesExactlyOneDiagnosticLine, :364 rejectionHandler(writingTo: logURL), :376 three tab fields. Gate :280 'public static func rejectionHandler(writingTo url: URL)'; :289 defaultOnReject is referenced in the two sink test files only inside a doc comment (LiveCredentialWriterTests.swift:385). Both tests passed in the task-3 and task-4 suite logs; CLAUDE_STATUS_BAR_HOME manifests before/after in task-3 (apppaths-manifest-*.txt, tester-manifest-*.txt) are 0 bytes and in task-4 (manifest-before.txt / manifest-after.txt) list only the manifests themselves plus swift-test.log, so no trusted-application-gate.log was written under the scratch root (N8)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C9 PACKAGE.SWIFT EXCLUDES THE FIXTURES",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e (`git diff --name-only 240588a..931079e -- Sources Tests Package.swift Makefile scripts .gitignore` prints nothing). Package.swift:31 'exclude: [\"Fixtures\"],' inside .testTarget StatusBarCoreTests; the epic diff on Package.swift is exactly that one insertion (numstat 1/0). `grep -n 'Fixtures/static-assertions'` on TrustedApplicationGateTests.swift: :747 (doc), :751 'testsDir.appendingPathComponent(\"Fixtures/static-assertions\")', :1540 '.appendingPathComponent(\"Fixtures/static-assertions\")' — still read by path. 16 fixture files present. Finding 0e5a7a62 is fix-verified. Build evidence: task-1 grader-r2.json C9; gate-outcome #195 and integration check #197 on 931079e ran swift build and the 434-test suite (pass)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C10 THE LIVING DOCUMENT",
      "status": "pass",
      "evidence": "Carried from verdict-v2; CLAUDE.md is not in the task-4 diff. CLAUDE.md (epic numstat 18/8): :90 'assigns each candidate path to one of two classes and verifies it against', :92-94 'app's own bundle path is the `.app` class; a path whose last component is `claude` is the `.claude` class; anything else is in neither class and is dropped', :94-95 'The `.claude` class requires Anthropic's Developer ID (team `Q6L2SF6YDW`) and bundle identifier `com.anthropic.claude-code`', :97 'The `.app` class requires the candidate to be \"signed the way this process is signed\"', :101 'or unsigned; under `swift run` that means the vault ACL repair is skipped'. `grep -n 'not a location check' CLAUDE.md`: no hit. '## Workflow' section present at :109, untouched."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C11 DEFAULTVERIFY IS WIRED END-TO-END FOR THE APP CLASS",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: TrustedApplicationGate.swift:178-179 'public static func defaultVerify(_ path: String) -> Bool {' / 'defaultOutcome(path) == .approved'. From v2: :68-71 'internal enum VerifyOutcome { case noSelf, selfFailsPrecondition, noDesignatedRequirement, ...}'; :158-161 'internal static func defaultOutcome(' ... 'acquireSelf: () -> SecStaticCode? = copySelfStaticCode'; :167 'appClassOutcome(candidatePath: path, selfCode: acquireSelf())'. Test :457 c11p1DefaultOutcomeOnSelfMatchesMeasurement; measureSelf runs codesign ['-dv'] at :431 and ['-d', '-r-'] at :432; :462 'print(\"P1 branch: approved\")' / :463 '#expect(outcome == .approved)'; :465-466 the selfFailsPrecondition branch. task-3 swift-test.txt:2153 'P1 branch: approved'; the test passed in the task-3 and task-4 logs. Instrument deviation carried in gaps: hasCertificate is computed at :447-449 via SecStaticCodeCheckValidity against selfCertificatePrecondition, not the 'Authority=' grep, and no 'codesign -v -R=<measured DR>' confirmation runs."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C12 THE SELF/CANDIDATE SWAP",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. TrustedApplicationGateTests.swift:474 c12p2SelfCandidateSwapCaughtByAdHocSpecimen; :477-481 skip branch 'probe skipped: self is ad-hoc, swap unobservable' as print + withKnownIssue + return (task-3); S4 = appClassSpecimen(sign: '-', ...) after the guard; :486 '#expect(outcome == .candidateRejected)' on defaultOutcome(S4, appBundlePath: S4) with real self-acquisition. Passed in the task-3 and task-4 logs with 0 'probe skipped' / 'recorded a known issue' lines (self carried a certificate: 'P1 branch: approved')."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C13 THE ACQUISITION-ERROR PATH AND THE BOOL REDUCTION",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Gate: TrustedApplicationGate.swift:79 'internal static func copySelfStaticCode() -> SecStaticCode?', :82 'guard selfStatus == errSecSuccess, let selfCode else { return nil }', :85 'guard staticStatus == errSecSuccess, let staticCode else { return nil }'. Tests: :251 c13p3NilSelfYieldsNoSelf, :253 'acquireSelf: { nil }', :254 '#expect(outcome == .noSelf)'; :259 c13p3AcquisitionCallsAreGuardedNotForceUnwrapped, :266 guardedCalls list, :272 pinned text 'SecCodeCopyStaticCode result used without an errSecSuccess guard at TrustedApplicationGate.swift:<lineNumber>'; :282 c13p4DefaultVerifyIsExactlyTheOutcomeReduction, :302 'defaultOutcome(p, appBundlePath: Bundle.main.bundlePath) == .approved'. All three passed in the task-3 and task-4 suite logs."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C1 THE HARNESS NEVER DELETES THE OPERATOR'S BUILD",
      "status": "pass",
      "evidence": "Carried from verdict-v2; scripts/ is not in the task-4 diff. Spot-checked on 931079e: `git show 931079e:scripts/signing-argv-test.sh | grep -nE 'rm|find'`, non-comment lines, contain 0 lines naming 'dist'; :383 '# ff55736f: never touch the repository's own dist/ here -- launch a copy'; the only non-comment 'dist' reference is :400 '[[ ! -e \"$SCAFFOLD/dist\" ]]' (a scaffold path). Base :356 'find dist -depth -exec rm ...' is gone. Behavioural sentinel: make signing-test is prohibited for this dispatch; state/results/task-2-signing-harness-keeps-dist-and-escapes-argv.grader-r1.json C1 records SENTINEL-SURVIVED and 'signing-argv-test: all checks passed', and integration check #197 on 931079e ran signing-test-keeps-dist with SENTINEL-SURVIVED (pass: true)."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C2 THE (e) REFUSAL CHECK STILL GRADES WHAT IT GRADED",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: scripts/signing-argv-test.sh:389 'cp \"$APPFILE\" \"$SCAFFOLD/scripts/make-app.sh\"' and :400 '[[ \"$E5RC\" -ne 0 ]] && has \"make-app: refusing to build a real artifact with CSB_SIGNING_DRY_RUN set\" \"$E5OUT\" && ! has 'Compiling' \"$E5OUT\" && [[ ! -e \"$SCAFFOLD/dist\" ]]'. From v2: :387 'SCAFFOLD=\"$(mktemp -d)\"'; :388 'mkdir -p \"$SCAFFOLD/scripts\"'; :391 '( \"${CLEAN[@]}\" CSB_SIGNING_DRY_RUN=1 /bin/bash \"$SCAFFOLD/scripts/make-app.sh\" ... & PID=$!' with the watchdog; :403 removes the scaffold. grader-r1.json C2: the harness printed 'ok: (e) behavioural (clean launch, watchdog): make-app refuses to build with CSB_SIGNING_DRY_RUN set'; integration check #197 on 931079e: signing-test pass."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C3 THE DRY-RUN PRINTER ESCAPES",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: scripts/lib/signing-run.sh:18 'printf \"csb-dry-run-argv: %q\\n\" \"$token\"' and scripts/signing-argv-test.sh:304 'check \"(d) newline token is printed as one escaped line\" $?' (bare description). From v2: signing-run.sh:8-12 header says tokens print %q-quoted one per line; :23 '\"$@\"' execute path unchanged; harness :303 asserts 2 lines, every line prefixed 'csb-dry-run-argv: ', no bare 'b'. Direct dry-run measurement recorded in verdict-v1 on the byte-identical file; grader-r1.json C3: 'ok: (d) newline token is printed as one escaped line'."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C4 NOTHING ELSE MOVES",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. `git diff --name-only 7d530e5..2493d04` lists exactly scripts/lib/signing-run.sh and scripts/signing-argv-test.sh; `git diff --shortstat 7d530e5..2493d04` = 42 insertions, 7 deletions (49 <= 200). scripts/make-app.sh and scripts/ensure-signing-identity.sh have no diff in 69f7613..931079e (the 11-file epic list names neither). Non-comment 'security' invocations in the harness: only :26 'security find-certificate -c \"ClaudeStatusBar Local Signing\"'. '\"${CLEAN[@]}\"' launches: exactly :226 (B1) and :391 ((e)). :460 still ends with 'signing-argv-test: all checks passed' when FAILURES == 0. grader-r1.json C4 and integration check #197 (signing-test pass on 931079e)."
    },
    {
      "task_id": "csb-signing-policy-1/task-3-known-issue-skips-and-gate-doc",
      "criterion": "C1 KNOWN-ISSUE SKIPS",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: TrustedApplicationGateTests.swift:145 'print(\"probe skipped: signing identity absent\")' :146 'withKnownIssue(\"probe skipped: signing identity absent\") {' :147 'Issue.record(\"probe skipped: signing identity absent\")' :148 '}' :149 'return'; `grep -c withKnownIssue` on the file at 931079e = 4; `git grep -c withKnownIssue 931079e -- Sources` matches no file. From v2: the task-3 diff is four hunks of three added lines each inside existing guard else-branches (:146-148, :210-212 'probe skipped: no Developer ID claude binary', :338-340, :478-480 'probe skipped: self is ad-hoc, swap unobservable'); `grep -c 'probe skipped: '` = 12; Package.swift:13 pins swift-testing exact 0.12.0 and .build/checkouts/swift-testing is tagged 0.12.0 (re-checked: `git describe --tags` = 0.12.0); its KnownIssue.swift:115-120 declares withKnownIssue(_ comment:, isIntermittent:, sourceLocation:, _ body:)."
    },
    {
      "task_id": "csb-signing-policy-1/task-3-known-issue-skips-and-gate-doc",
      "criterion": "C2 LOCAL RUN STAYS GREEN AND OBSERVABLE",
      "status": "pass",
      "evidence": "Not re-run here (prohibited). Evidence on the final tree: task-3 grader-r1.json C2 and task-3 tester.json (bc4a3fc: 'Test run with 434 tests passed after 3.178 seconds.' / '... 3.233 seconds.'); task-4 coder swift-test.txt at 3c1dc1b (== 931079e tree): '✔ Test run with 434 tests passed after 3.223 seconds.' with 0 'recorded a known issue' lines (counted by me in state/artifacts/.../task-4-ci-observable-known-issues/swift-test.txt) and known_issues_recorded: 0 in its structured_output; the task-4 grader's own run 'Test run with 434 tests passed after 3.170 seconds.' with grep -c 'recorded a known issue' = 0; integration check #197 on 931079e 'Test run with 434 tests passed'. Identity PRESENT on this host (read-only find-certificate exit 0 in this dispatch) and a qualifying claude binary is installed, so no branch fired; a non-firing withKnownIssue produced no issue, which is what the criterion demands of the local run. Manifests (task-3 apppaths-manifest-*/tester-manifest-* 0 bytes; task-4 manifest-before/after list only the manifests and swift-test.log)."
    },
    {
      "task_id": "csb-signing-policy-1/task-3-known-issue-skips-and-gate-doc",
      "criterion": "C3 GATE DOC COMMENT STATES (i)-(iii)",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. Spot-checked on 931079e: TrustedApplicationGate.swift:203 '/// The two sinks that call this (LiveCredentialWriter.swift,' :205 '/// of this filtered result: (i) an empty filtered list (`trustedPaths:' :211 '/// existing item and its ACL untouched; (iii) `SecAccessCreate` only' :214 'public static func filter('. From v2 (full block :191-213 quoted there): it states (i) empty filtered list -> write without kSecAttrAccess (update preserves the existing ACL, add gets the system default), (ii) non-empty filtered list whose every path fails to become a SecTrustedApplication -> refuse rather than SecAccessCreate with an empty array, (iii) SecAccessCreate only with a non-empty array; keeps the four existing statements; names both sink files without line numbers; the task-3 diff on this file is 12 added '///' lines and 0 other changes (numstat 12/0), so the filter signature/body is byte-identical. Deviation (appended, not substituted; the surviving :197-198 'system-default-ACL' sentence is loose for the update path) carried as a low-confidence gap."
    },
    {
      "task_id": "csb-signing-policy-1/task-3-known-issue-skips-and-gate-doc",
      "criterion": "C4 NO COLLATERAL CHANGE",
      "status": "pass",
      "evidence": "Carried from verdict-v2; unmoved at 931079e. `git diff --numstat 2493d04..240588a`: '12 0 Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift' and '12 0 Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift' — exactly the two claimed files. `grep -c '@Test'` on the test file: 52 at 2493d04, 52 at 240588a, 52 at 931079e (re-measured); the task-3 diff contains 0 lines matching '@Test' and 0 matching '#expect'/'#require'. 'trustedPaths: []' sites: 14 across LiveCredentialWriterTests.swift (:87,:154,:185,:202,:217,:238,:255,:344) and AccountCredentialVaultTests.swift (:183,:199,:210,:221,:305,:319), none touched by task-3 or task-4. Manifests: apppaths-manifest-before/after.txt and tester-manifest-before/after.txt all 0 bytes."
    },
    {
      "task_id": "csb-signing-policy-1/task-3-known-issue-skips-and-gate-doc",
      "criterion": "C5 SIZE",
      "status": "pass",
      "evidence": "Carried from verdict-v2. `git diff --numstat 2493d04..240588a` sums to 24 added + 0 removed = 24 <= 120 (hard cap 200). `git log --oneline 2493d04..240588a` lists the merge 240588a and exactly one task commit bc4a3fc 'Route probe-skip branches through withKnownIssue; document sink ACL policy in filter doc comment' (English). task-3 grader-r1.json C5 records no upstream configured for the task branch; `git branch -r` on the checkout lists no smith/csb-signing-policy-1/* branch at all (re-measured in this dispatch)."
    },
    {
      "task_id": "csb-signing-policy-1/task-4-ci-observable-known-issues",
      "criterion": "C1 CAPTURE WITHOUT CHANGING THE GATE",
      "status": "pass",
      "evidence": "Measured on 931079e (`git show 931079e:.github/workflows/ci.yml | cat -n`): :11 '- name: Swift test' :12 'shell: bash' :13 'run: swift test 2>&1 | tee swift-test.log' — the only 'run: swift test' line in the file (`grep -c '^ *run: swift test'` = 1; the literal 'swift test' also occurs at :20 in the name 'Upload swift test log'). PyYAML (6.0.2) parses that step as exactly {'name': 'Swift test', 'shell': 'bash', 'run': 'swift test 2>&1 | tee swift-test.log'} — no `if`, no `continue-on-error` (`grep -c continue-on-error` on the whole file = 0), no `working-directory`; `swift test` carries no flag and is the Makefile `test` target's command (Makefile:6-7 'test:' / 'swift test' at 931079e). `git diff 240588a..931079e` (the whole task-4 diff, +27 -1, one file) has exactly one '-' line, '-      - run: swift test' (240588a :11); its '+' lines are the three-line suite step and the two appended steps at :20-43; :10 '- uses: actions/checkout@v4', :14-17 'Hook integration test' ('swift build' / './scripts/hook-integration-test.sh') and :18-19 'Signing argv test' ('make signing-test') are byte-identical to 240588a :10, :12-15 and :16-17 (no -/+ line inside them); :1-8 'name: CI', 'on:' pull_request + push branches [main] (PyYAML: {'pull_request': None, 'push': {'branches': ['main']}}) and 'runs-on: macos-15' unchanged. GitHub runs an explicit `shell: bash` as `bash --noprofile --norc -eo pipefail {0}`, so the step's status is the pipeline's under pipefail — the probe under C3 shows that is `swift test`'s status, not tee's."
    },
    {
      "task_id": "csb-signing-policy-1/task-4-ci-observable-known-issues",
      "criterion": "C2 ARTIFACT AND ANNOTATIONS, ALWAYS, OUTCOME-NEUTRAL",
      "status": "pass",
      "evidence": "Measured on 931079e. (a) :20-26 '- name: Upload swift test log' / 'if: always()' / 'uses: actions/upload-artifact@v4' / 'with:' / 'name: swift-test-log' (fixed) / 'path: swift-test.log' / 'if-no-files-found: warn' (PyYAML step 4: {'name': 'Upload swift test log', 'if': 'always()', 'uses': 'actions/upload-artifact@v4', 'with': {'name': 'swift-test-log', 'path': 'swift-test.log', 'if-no-files-found': 'warn'}}). (b) :27-43 '- name: Annotate recorded known issues' / 'if: always()' / 'shell: bash' / 'run: |' with the body, quoted verbatim from `git show 931079e:.github/workflows/ci.yml` :31-43 (de-indented): \"matches=$(grep -F 'recorded a known issue' swift-test.log || true)\" / \"count=0\" / \"if [ -n \\\"$matches\\\" ]; then\" / \"  while IFS= read -r line; do\" / \"    echo \\\"::warning title=swift-testing known issue::${line}\\\"\" / \"    count=$((count + 1))\" / \"  done <<< \\\"$matches\\\"\" / \"fi\" / \"summary=$(grep -F 'Test run with' swift-test.log | tail -n 1 || true)\" / \"{\" / \"  echo \\\"## swift-testing known issues: ${count}\\\"\" / \"  if [ -n \\\"$summary\\\" ]; then echo \\\"$summary\\\"; else echo \\\"summary line not found\\\"; fi\" / \"} >> \\\"$GITHUB_STEP_SUMMARY\\\"\". It greps the literal 'recorded a known issue', prints one '::warning title=swift-testing known issue::<line>' per match, appends the count and the last 'Test run with' line (or 'summary line not found') to $GITHUB_STEP_SUMMARY; both greps are '|| true'-guarded. Whole-file greps at 931079e: 'continue-on-error' 0, '::error' 0, no line containing the word 'exit'. The PyYAML-extracted body is byte-identical to the coder artifact annotate.sh (478 bytes, `diff` clean). My own runs of that body under `bash --noprofile --norc -eo pipefail` with GITHUB_STEP_SUMMARY exported, from a cwd under $(mktemp -d) (never a repo path): fixture log containing the plan's line -> exit 0, exactly 1 '::warning title=swift-testing known issue::✘ Test defaultVerifyRejectsLocalIdentitySignedClaudeSpecimen() recorded a known issue at TrustedApplicationGateTests.swift:147:9: Issue recorded: probe skipped: signing identity absent', summary '## swift-testing known issues: 1' + '✔ Test run with 434 tests passed after 3.2 seconds with 1 known issue.'; three-match log -> exit 0, 3 warnings, count 3; empty log -> exit 0, 0 warnings, 'summary line not found'; missing log -> exit 0, 0 warnings (grep's 'No such file or directory' on stderr twice), 'summary line not found'. Grader-r1 C2/C3: (i) real 0 / (ii) fixture 1 / (iii) empty 0 / (v) missing 0 warnings, all exit 0. Tester fixture matrix (tester.json notes.fixture_results): zero/one/three/failing/mixed/empty/missing -> exit 0 each, warnings 0/1/3/0/1/0/0, and every emitted warning payload verified present verbatim in its source log. The two steps are placed after 'Signing argv test' (so the hook and signing steps stay byte-identical per C1); with `if: always()` they run whether the suite or a later step failed, and neither can fail the job (upload with if-no-files-found: warn, annotate exits 0 on every path), so the job's outcome stays whatever the earlier steps produced."
    },
    {
      "task_id": "csb-signing-policy-1/task-4-ci-observable-known-issues",
      "criterion": "C3 LOCAL PROOF OF BOTH BRANCHES AND OF THE EXIT-STATUS INVARIANT, RECORDED",
      "status": "pass",
      "evidence": "Not re-run here (swift test prohibited for this dispatch); graded on the recorded runs plus my own probes of the shell invariant. Coder Result state/results/task-4-ci-observable-known-issues.json structured_output: precondition 'PRESENT', known_issues_recorded 0, summary_line '✔ Test run with 434 tests passed after 3.223 seconds.', annotate_real_warnings 0, annotate_fixture_warnings 1, annotate_empty_warnings 0, pipefail_probe_exit 1, diff_numstat '27 1 .github/workflows/ci.yml'. Artifacts under state/artifacts/csb-signing-policy-1/task-4-ci-observable-known-issues/ read by me: swift-test.txt (226,410 bytes) contains 0 'recorded a known issue' lines and the summary line above; manifest-before.txt lists only itself, manifest-after.txt lists the two manifests and swift-test.log — nothing else was created under the scratch root by the suite (the criterion's 'nothing but the log' is met modulo the manifests being written into the same directory); annotate-real.txt 0 bytes, annotate-fixture.txt exactly the one ::warning line quoted under C2, annotate-empty.txt 0 bytes; summary-real.md, summary-fixture.md ('## swift-testing known issues: 1' / '✔ Test run with 434 tests passed after 3.223 seconds with 1 known issue.'), summary-empty.md present; fixture.log (226,616 bytes) and annotate.sh (478 bytes, identical to the committed body); pipefail-probe.txt 'outer_exit=1'. Independent single suite run in grader-r1.json C3: find-certificate PRESENT; `bash --noprofile --norc -eo pipefail ... swift test | tee swift-test.log` -> 'Test run with 434 tests passed after 3.170 seconds.', pipeline exit 0, grep -c 'recorded a known issue' = 0, manifest diff shows only swift-test.log; annotate runs (i) 0 / (ii) 1 / (iii) 0 warnings, exit 0 each. Pipefail probe, my own measurement in this dispatch (bash 3.2.57, the macOS /bin/bash): form1 exactly as written, `bash --noprofile --norc -eo pipefail -c 'false 2>&1 | tee \"$1\"; echo exit=$?' _ <tmp>/false.log` -> prints nothing, outer status 1 (-e ends the shell on the failing pipeline before the inner echo); form2 without -e (`-o pipefail`) -> prints 'exit=1', outer 0; the null (no pipefail at all) -> prints 'exit=0', outer 0, i.e. tee's status would mask a failing suite; `true 2>&1 | tee` under -eo pipefail -> outer 0. The tester recorded the same two forms in tester-pipefail.txt and the grader recorded outer=1 plus the -o-pipefail 'exit=1'. The property the criterion protects — the CI step's status is `swift test`'s, not tee's — holds on every measurement; the literal 'must print exit=1' is unreachable under -e as written and is recorded as a wording gap, not a fail (deviation 3)."
    },
    {
      "task_id": "csb-signing-policy-1/task-4-ci-observable-known-issues",
      "criterion": "C4 THE WORKFLOW PARSES AND NOTHING ELSE CHANGES",
      "status": "pass",
      "evidence": "Measured on 931079e: `python3 -c 'import yaml,sys; yaml.safe_load(open(sys.argv[1]))'` on the file extracted with `git show 931079e:.github/workflows/ci.yml` exits 0; sorted step names ['<unnamed>', 'Annotate recorded known issues', 'Hook integration test', 'Signing argv test', 'Swift test', 'Upload swift test log'] — exactly one unnamed step and it is {'uses': 'actions/checkout@v4'}; `git diff --numstat 240588a..931079e` is exactly '27\t1\t.github/workflows/ci.yml' (one line); `git diff --name-only 240588a..931079e -- Sources Tests Package.swift Makefile scripts .gitignore` prints nothing (.gitignore, dist/, .build/, Sources/, Tests/ untouched); `git show <sha>:Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift | grep -c '@Test'` = 52 at 240588a and 52 at 931079e. `git status --porcelain` in the task worktree at return is not re-measurable from this checkout (the integration branch); grader-r1 C4 graded it and gate-outcome #195 / wave-merged #196 record files_changed = .github/workflows/ci.yml only. Note: swift-test.log is not gitignored (`git show 931079e:.gitignore | grep -E 'log|swift-test'` = none) and C4 pins .gitignore untouched; in CI nothing commits or asserts a clean tree after the suite step (`git grep -nE 'porcelain|git status|git diff --quiet|untracked' 931079e -- scripts Makefile .github` = none), so the untracked log in the runner workspace is inert."
    },
    {
      "task_id": "csb-signing-policy-1/task-4-ci-observable-known-issues",
      "criterion": "C5 SIZE, COMMITS, NO PUSH",
      "status": "pass",
      "evidence": "Measured on the checkout: `git diff --numstat 240588a..931079e` sums to 27 + 1 = 28 <= 60 (hard cap 120); `git log --oneline 240588a..931079e` lists the merge 931079e 'Merge csb-signing-policy-1/task-4-ci-observable-known-issues into smith/csb-signing-policy-1/integration' and exactly one task commit 3c1dc1b 'ci: capture swift test output and annotate recorded known issues' (English); `git log -1 --format=%p 931079e` = 240588a 3c1dc1b; the local branch smith/csb-signing-policy-1/task-4-ci-observable-known-issues points at 3c1dc1b; `git branch -r | grep -c 'smith/csb-signing-policy-1/task-4'` = 0 (the remote lists no smith/* branch at all); `git rev-parse --abbrev-ref --symbolic-full-name 'smith/csb-signing-policy-1/task-4-ci-observable-known-issues@{u}'` -> 'fatal: no upstream configured for branch ...' (rc 128). Ledger: budget-check-result #192 (47,359 tokens, 28 diff lines, no overrun)."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-1: The successor epic `csb-audit-1` named when it closed (ledger `csb-audit-1-2026-09-07#338`, `successor_epic`), carrying the one S2 that epic left standing.",
      "status": "pass",
      "evidence": "The carried S2 (a locally-minted certificate admitted as a trusted application for the credential item) is closed on 931079e: TrustedApplicationGate.swift:35-36 pins the OU-bearing claudeRequirement and TrustedApplicationGateTests.swift:143-155 rejects the exact S2 specimen (local identity, '-i com.anthropic.claude-code'); the probe ran on this host (identity PRESENT; 0 'recorded a known issue' lines in task-3 and task-4 suite logs; the test passed), a host where it cannot run records a known issue instead of a plain pass (:146-148, task-3), and since task-4 a CI run that records one surfaces it as a '::warning' annotation, a step-summary count and the swift-test-log artifact (.github/workflows/ci.yml:20-43) rather than in console text that JUnit XML drops. Integration lineage: 931079e = merge(3c1dc1b) <- 240588a = merge(bc4a3fc) <- 2493d04 = merge(6bebbaf, b3934a5) <- 7d530e5 = merge(f7a3cc3) <- 69f7613 (csb-audit-1 integration tip; `git log --first-parent` confirms)."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-2: `csb-audit-1` task-2d added `TrustedApplicationGate` in front of the Keychain ACL grant, but its single `policyRequirement` string admits any Developer-ID-signed binary on the machine as a trusted application for the credential item — far weaker than \"the `claude` binary or this app\", which is what the grant is for.",
      "status": "pass",
      "evidence": "The single string is gone: `grep -c policyRequirement` on Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift at 931079e = 0. Replacement is per class: :57-59 classify; :132-137 outcome switch (.claude -> claudeRequirement, .app -> appClassOutcome, nil -> candidateRejected) and the same switch inside defaultOutcome at :163-168. 'certificate leaf[subject.CN] exists' survives only as selfCertificatePrecondition (:44), applied to SELF at :120-121, never to a candidate. TrustedApplicationGateTests.swift:223 asserts defaultVerify('/bin/ls') == false (spot-checked at 931079e), :178 rejects an Apple-signed /bin/ls copy named 'claude'."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-3: The operator confirmed the finding and chose to land the gate as it was (better than no gate: it still refuses ad-hoc and unsigned binaries) and to fix the policy here rather than amend that plan a seventh time, because criteria 6 and 12 of task-2d had just pinned the requirement string and a new one needs its own specimens and its own grading.",
      "status": "pass",
      "evidence": "The new policy has its own specimens and grading on 931079e: claude-class specimens at TrustedApplicationGateTests.swift:143-178 (local-identity, ad-hoc, Apple-signed wrong identifier), app-class S1-S4 at :335-367, real-binary probe :186-215; the pinned literals are re-pinned at :36 (claudeRequirement) and :39 selfCertificatePreconditionIsExactlyPinnedLiteral. The refusals the operator relied on are retained (:76 defaultVerifyRejectsAdHocSignedCode, :238 defaultVerifyRejectsUnsignedDirectory). Grading history: task-1 grader r1 fail -> r2 pass; task-3 grader r1 pass; task-4 grader r1 pass 5/5; four full 434-test runs on the final Swift tree (task-3 swift-test.txt, tester-swift-test.txt, task-4 swift-test.txt, task-4 grader run) plus integration check #197 on 931079e."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-4: Measured design direction: per-path-class policy — for the `claude` binary, `anchor apple generic and identifier \"com.anthropic.claude-code\"` plus `certificate leaf[subject.OU] = \"Q6L2SF6YDW\"`; for the app's own self-signed bundle, the running process's own designated requirement (a pinned leaf-certificate hash is the fallback if a self-signed DR does not validate without a trusted anchor); a path in neither class is refused.",
      "status": "pass",
      "evidence": "Graded as written. TrustedApplicationGate.swift:35-36 at 931079e is exactly the clause's string (spot-checked; csreq -r - -t exit 0 in the v2 dispatch); 'codesign -v -R=<that string>' on ~/.local/bin/claude (Identifier=com.anthropic.claude-code, TeamIdentifier=Q6L2SF6YDW) exited 0 in the v2 dispatch, so the pin is satisfied by the shipped binary. App class uses SecCodeCopyDesignatedRequirement of self at :124-128 with no hash in source (grep for 'H\"'/'cdhash' in the gate file finds only the :40/:112 doc comments explaining the ad-hoc case); the primary DR path validated on this host ((4a) at test :358 passed with identity-signed S1/S2), so the hash fallback was not needed. Neither class -> .candidateRejected (:137-138, :168-169; tests :46, :223). The security-reviewer's Developer-ID-intermediate pin candidate is outside the clause as written; the operator waived it as S4 24bc3394 (ledger #129) — carried in gaps as a note, not a fail."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-5: The same epic closes the empty-ACL hole at both `SecAccessCreate` sites (a rejected write must leave the old ACL alone, never write an item that names zero trusted applications) and fixes the signing test harness so it never deletes the operator's `dist/` and escapes what it prints.",
      "status": "pass",
      "evidence": "Both SecAccessCreate sites guarded at 931079e: `git grep -n 'SecAccessCreate(' 931079e -- Sources` finds only LiveCredentialWriter.swift:152 and AccountCredentialVault.swift:224, each inside 'if !filteredPaths.isEmpty {' after 'guard !trustedApps.isEmpty else { ... return false }' (:138-152 and :212-224); the all-rejected filter (nil) returns false before any update/add at LiveCredentialWriter.swift:124-131 and AccountCredentialVault.swift:199-206. Finding 28a322bb is fix-verified. Tests at both sinks: LiveCredentialWriterTests.swift:340/:360/:388, AccountCredentialVaultTests.swift:316/:332/:353. The policy is documented in the gate file itself (TrustedApplicationGate.swift:203-213, spot-checked at 931079e). Harness: 0 non-comment rm/find lines name 'dist' (re-measured at 931079e); scaffold copy at scripts/signing-argv-test.sh:383-403 (:389 cp, :400 assertion spot-checked); printer scripts/lib/signing-run.sh:18 printf %q (spot-checked) with the (d) check at :303-304; integration check #197 on 931079e: signing-test-keeps-dist SENTINEL-SURVIVED and signing-test pass."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-6: Scoped like its parent: no UI, no `AppState`, no self-heal or locator files, and every agent bound by the same prohibition on mutating `/usr/bin/security` subcommands — the blast radius is the operator's real login keychain.",
      "status": "pass",
      "evidence": "`git diff --name-only 69f7613..931079e` lists exactly 11 files: .github/workflows/ci.yml, CLAUDE.md, Package.swift, the three Accounts/ sources (AccountCredentialVault, LiveCredentialWriter, TrustedApplicationGate), the three StatusBarCoreTests files, scripts/lib/signing-run.sh, scripts/signing-argv-test.sh — no AppState, no self-heal, no locator, no UI path (`git diff --shortstat 69f7613..931079e` = 11 files, 1078 insertions, 82 deletions). Added lines of that diff matching 'security (import|set-key-partition-list|create-keychain|delete-keychain|add-generic-password|unlock-keychain|default-keychain|delete-certificate)': 0 (re-measured). Subprocess launches in the test file: /usr/bin/codesign at :62,:82,:131,:194,:422 and /usr/bin/security only at :101 (find-certificate, read-only); the harness's only security invocation is :26 find-certificate; the workflow runs only swift test / swift build / the two existing scripts / grep. Reviewer and security-reviewer files for task-1, task-2, task-3 and task-4 are empty finding lists (task-4's security-reviewer was dispatched because .github/workflows/** is a sensitive path; its injection probe ran under $(mktemp -d) only). This dispatch ran only read-only find-certificate (exit code, output discarded), git, grep, sed, awk, python3/PyYAML, diff, ls and the annotation body under $(mktemp -d)."
    },
    {
      "task_id": "epic",
      "criterion": "amendment-discharge: task-4 discharges finding d5c7c55a (S2, task-3 C1: a withKnownIssue skip that fires in CI is recorded only in swift test's console text, which JUnit XML drops)",
      "status": "pass",
      "evidence": "What a fired skip prints, read from the pinned swift-testing 0.12.0 checkout (Package.swift:13 exact 0.12.0; .build/checkouts/swift-testing `git describe --tags` = 0.12.0): Event.HumanReadableOutputRecorder.swift:405-408 sets symbol = .pass(knownIssueCount: 1) and known = ' known' when issue.isKnown, and :439 renders '<Test> <name>() recorded a<known> issue<atSourceLocation>: <issue.kind>'; Event.Symbol.swift:111-114 renders .pass(knownIssueCount > 0) as HEAVY BALLOT X (✘). So the branch at TrustedApplicationGateTests.swift:145-149 emits exactly the shape the plan names — '✘ Test defaultVerifyRejectsLocalIdentitySignedClaudeSpecimen() recorded a known issue at TrustedApplicationGateTests.swift:147:9: Issue recorded: probe skipped: signing identity absent' — and Event.HumanReadableOutputRecorder.swift:140-142 appends ' with N known issue(s)' to the 'Test run with' summary. Why it was invisible: Event.JUnitXMLRecorder.swift:124-127 'case let .issueRecorded(issue): if issue.isKnown { return nil }' — confirmed in the checkout. What task-4 does with it (.github/workflows/ci.yml at 931079e): :13 captures the console stream (stderr included via 2>&1) into swift-test.log; :20-26 uploads it as artifact 'swift-test-log' on every outcome; :31-37 lifts each 'recorded a known issue' line into '::warning title=swift-testing known issue::<line>'; :39-43 appends '## swift-testing known issues: <count>' and the 'Test run with ... with N known issue(s)' line to $GITHUB_STEP_SUMMARY. For the fixture line above the emitted annotation is, measured by me in this dispatch under bash --noprofile --norc -eo pipefail: '::warning title=swift-testing known issue::✘ Test defaultVerifyRejectsLocalIdentitySignedClaudeSpecimen() recorded a known issue at TrustedApplicationGateTests.swift:147:9: Issue recorded: probe skipped: signing identity absent' with summary '## swift-testing known issues: 1' / '✔ Test run with 434 tests passed after 3.2 seconds with 1 known issue.'. On GitHub that surfaces in three places: the warning annotation appears in the run's Annotations panel on the workflow-run summary page and on the job page under the 'Annotate recorded known issues' step (a job-level annotation, since no file=/line= properties are set; it is also listed on a PR's Checks tab); the two summary lines appear in the job's step summary block on the run summary page; and swift-test.log is downloadable from the run's Artifacts section as 'swift-test-log'. Proven by fixture (this dispatch, the coder, the grader and the tester's 7-case matrix): the annotation body's behaviour for 0/1/3 matches, a failing summary, an empty log and a missing log under the same shell flags GitHub uses, each exiting 0; the pipefail invariant of the capture step; PyYAML's structural parse; and, from swift-testing's source, that the grep literal is the text a fired branch actually prints. Not proven, because nothing here can run the workflow and the identity is PRESENT on this host: that a macos-15 runner's `swift test` writes that line into swift-test.log (it follows from the pinned toolchain but has not been observed), that GitHub renders the '::warning' as an annotation and honours the step summary, and that upload-artifact@v4 succeeds — all platform features, not code this epic wrote. Decision: the finding asked for the recorded skip to be observable where it fires, and the committed workflow makes it observable through the swift-testing text that is deterministic under the pinned 0.12.0; the fixture proof is sufficient to call d5c7c55a discharged. The first real CI run remains unobserved and is carried as a gap below 0.8 (an operator observation after merge, not a task inside this epic's claims)."
    }
  ],
  "overall": "pass",
  "gaps": [
    {
      "criterion_ref": "task-1 plan constraint: diff cap",
      "description": "task-1 landed 985 insertions + 74 deletions = 1059 changed lines against the plan's 900-line cap (`git diff --shortstat 69f7613..7d530e5`, re-measured on the 931079e checkout); the overrun was recorded and the gate passed at ledger #108. No code gap follows — the overrun is test-side (TrustedApplicationGateTests.swift +586 in the epic diff) — but it is a planning lesson: C6's test-side closure work and the C11-C13 wiring probes were enough for a sibling task. Carried from v1/v2 unchanged; the operator has seen it.",
      "confidence": 1.0,
      "suggested_task": null
    },
    {
      "criterion_ref": "task-1 C11 (P1 instrument)",
      "description": "selfHasCertificate is derived from SecStaticCodeCheckValidity against selfCertificatePrecondition (TrustedApplicationGateTests.swift:447-449, unmoved at 931079e, with a comment on why the 'Authority=' text heuristic misreads a CommandLineTools helper) rather than the 'Authority=' grep the criterion names, and the approved branch does not run the 'codesign -v -R=<measured DR>' confirmation. The measurement is stronger than the letter (it tests the exact predicate step (ii) applies) but is not independent of the Security-framework call the gate uses; the codesign -dv and -d -r- lines are still recorded (:431-432). Carried from v1/v2.",
      "confidence": 0.3,
      "suggested_task": null
    },
    {
      "criterion_ref": "goal-clause-4 (spirit; security-reviewer spec-change candidate, waived S4 24bc3394)",
      "description": "claudeRequirement (TrustedApplicationGate.swift:35-36) pins 'anchor apple generic', the identifier and leaf[subject.OU] but not the Developer ID intermediate ('certificate 1[field.1.2.840.113635.100.6.2.6]') that Apple's own DR for the shipped binary carries, so an Apple-anchored certificate of another type issued to team Q6L2SF6YDW would also satisfy the claude class. The clause as written is met and the operator waived this as S4 24bc3394 at ledger #129 as a roadmap follow-up; recorded so the verdict does not read as if the question were unexamined. No task in this epic.",
      "confidence": 0.3,
      "suggested_task": null
    },
    {
      "criterion_ref": "amendment-discharge d5c7c55a / task-4 C1-C2 (first CI run not yet observed)",
      "description": "Replaces the v2 gap 'task-3 C2 absent branch never observed' (confidence 0.5), which I close: task-4 removes the half of that gap that mattered — a fired skip was invisible where it fires — and the fixture proof (this dispatch, coder, grader, tester matrix) plus swift-testing 0.12.0's source shows the committed body turns the exact line a fired branch prints into an annotation, a summary count and an artifact. What remains unobserved is narrower: no workflow run has yet executed the new steps on a GitHub macos-15 runner (nothing here can trigger one, the task branch was never pushed, and the identity is PRESENT locally so no branch fires here), so the 'passed ... with 1 known issue' summary and the three '::warning' annotations for the identity/binary-gated tests have been reproduced by fixture, not seen on the Actions UI. This is an operator observation after the integration branch reaches GitHub, not a task inside this epic's claims (workers may not push).",
      "confidence": 0.35,
      "suggested_task": "After the integration branch is pushed, open the first CI run and record in the epic ledger the Annotations panel entries titled 'swift-testing known issue', the step summary's '## swift-testing known issues: N' line and the presence of the swift-test-log artifact, closing d5c7c55a by observation."
    },
    {
      "criterion_ref": "task-4 C3 (iv) wording (pipefail probe 'prints exit=1')",
      "description": "The criterion's literal probe `bash --noprofile --norc -eo pipefail -c 'false 2>&1 | tee f; echo exit=$?'` cannot print 'exit=1': under -e the shell exits on the failing pipeline before the inner echo runs, and the observable is the outer status 1 (measured by me, the coder, the grader and the tester; 'exit=1' prints only under -o pipefail without -e). The property the criterion protects — the CI step's status is swift test's, not tee's — holds and its null (no pipefail -> 'exit=0') was also measured. Per lesson-dream-dogfood-demo-rpg-1-182 this is an appended note on an already-graded criterion, not a reason to supersede its text; no task.",
      "confidence": 0.15,
      "suggested_task": null
    },
    {
      "criterion_ref": "task-3 C3 (letter: 'rewrite' vs append)",
      "description": "The (i)-(iii) paragraph was appended (TrustedApplicationGate.swift:202-213, 12 added lines, 0 removed; unmoved at 931079e) rather than the block being rewritten; the surviving sentence at :197-198 'An empty input returns `[]`, preserving today's system-default-ACL behaviour' is compatible with (i) but imprecise for the update path, where the existing ACL is preserved rather than a system default applied. The new paragraph immediately below states the precise rule, so a reader is not misled; substance met. Carried from v2.",
      "confidence": 0.2,
      "suggested_task": null
    },
    {
      "criterion_ref": "task-2 C3 (cosmetic)",
      "description": "The new harness check reuses the letter '(d)' (scripts/signing-argv-test.sh:304, unmoved at 931079e) already used by the pre-existing '(d) no-leak' check in another section; the plan pinned the description text, so the coder left it. Two 'ok: (d) ...' lines print per run. Cosmetic; carried from v1/v2.",
      "confidence": 0.3,
      "suggested_task": null
    }
  ],
  "next_plan_version": null,
  "notes": [
    "Carried criteria: task-1, task-2 and task-3 are graded on the same tree as v2 because `git diff --name-only 240588a..931079e` is exactly .github/workflows/ci.yml; at least one quoted line per task was re-read with `git show 931079e:<path>` and is recorded inline as 'spot-checked on 931079e'.",
    "Deviation 5 (orchestrator brief wording): task-4 C4 was graded at 52 @Test on Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift by the grader, the tester and this verdict (the 434 figure is the suite total, not the pinned file); no criterion was graded against the wrong number. The reviewer envelope's reference to a non-existent factory/policies/stack-policies.yml changed no outcome (0 findings on a 28-line YAML diff that all three judges and I read in full).",
    "Event #174 (duplicate grader declaration under the wrong plan version) was ignored per the brief; #175 is the declaration graded against.",
    "Finding set at close: 28a322bb and 0e5a7a62 fix-verified; 5a2a64b6, e17913e2 (task-3) and d5c7c55a (task-4) amend-pending -> discharged by this verdict; 24bc3394 and 291a06a6 waived. No new finding on task-4."
  ]
}
