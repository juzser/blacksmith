{
  "plan_version": 1,
  "epic_id": "csb-signing-policy-1",
  "integration_sha": "2493d04",
  "criteria": [
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C1 STRUCTURE AND MAPPING",
      "status": "pass",
      "evidence": "Graded diff is 69f7613..2493d04 (the brief's 'base b3934a5' is task-2's branch tip; `git log --graph` shows 7d530e5^1 = 2493d04^1 = 69f7613). Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift:25 'public enum PathClass {' :26 'case app' (:27 case claude); :35-36 'public static let claudeRequirement = \"anchor apple generic and identifier \\\"com.anthropic.claude-code\\\" and certificate leaf[subject.OU] = \\\"Q6L2SF6YDW\\\"\"' (csreq -r - -t on that literal exits 0); :44 'public static let selfCertificatePrecondition = \"certificate leaf[subject.CN] exists\"'; :57-59 'public static func classify(_ path: String, appBundlePath: String) -> PathClass?' with 'if path == appBundlePath { return .app }' then 'if URL(fileURLWithPath: path).lastPathComponent == \"claude\" { return .claude }' else nil. `grep -n policyRequirement` on the gate file: no hits (deleted; base :38 had it). Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift:36 pins the OU literal, :44-47 assert the four mapping cases ('/tmp/A.app','/tmp/A.app')->.app, ('/tmp/x/claude','/tmp/A.app')->.claude, ('/bin/ls','/tmp/A.app')->nil, ('/tmp/x/claude','/tmp/x/claude')->.app. Removed #expect lines in the test diff are exactly 3: the old policyRequirement literal, the self-signed-accepted assertion (C5e), and defaultVerify(\"/bin/ls\") == true (C5d)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C2 CLAUDE CLASS REJECTS WHAT THE OLD POLICY ADMITTED",
      "status": "pass",
      "evidence": "TrustedApplicationGateTests.swift:143 defaultVerifyRejectsLocalIdentitySignedClaudeSpecimen: :149 codesign ['--force','--sign','ClaudeStatusBar Local Signing','-i','com.anthropic.claude-code'], :152 '#expect(TrustedApplicationGate.defaultVerify(specimen.path) == false)'; presence decided at :101-104 by /usr/bin/security find-certificate -c \"ClaudeStatusBar Local Signing\" <home>/Library/Keychains/login.keychain-db (the pinned read-only command); skip line :145 'probe skipped: signing identity absent'. :156 defaultVerifyRejectsAdHocSignedClaudeSpecimen signs '--sign -' (:158) and asserts false (:161). :168 defaultVerifyRejectsAppleSignedWrongIdentifierClaudeSpecimen copies /bin/ls (:174) to <tmp>/claude and asserts false (:175). On this machine find-certificate exits 0 (measured read-only), and state/results/task-1-per-path-class-signing-policy.grader-r2.json C2 records all three tests passed with 0 'probe skipped' lines; round2-swift-test.txt artifact: 0 'probe skipped' lines, 'Test run with 434 tests passed'. Letter deviation: the skip path is print+return, not withKnownIssue (see gaps)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C3 CLAUDE CLASS ADMITS THE REAL BINARY",
      "status": "pass",
      "evidence": "TrustedApplicationGateTests.swift:183 defaultVerifyAcceptsRealClaudeBinaryIfPresent: :184-185 iterates LiveCredentialWriter.claudeBinaryCandidates plus NSHomeDirectory()-expanded ~/.local/bin/claude, :198 gates on 'TeamIdentifier=Q6L2SF6YDW' from codesign -dv, skip line :206 'probe skipped: no Developer ID claude binary'. Read-only measurement on the integration host: codesign -dv ~/.local/bin/claude -> Executable=~/.local/share/claude/versions/2.1.269, Identifier=com.anthropic.claude-code, TeamIdentifier=Q6L2SF6YDW; DR 'identifier \"com.anthropic.claude-code\" and anchor apple generic and certificate 1[field.1.2.840.113635.100.6.2.6] and certificate leaf[field.1.2.840.113635.100.6.1.13] and certificate leaf[subject.OU] = Q6L2SF6YDW'. LiveCredentialWriter.swift:253 claudeBinaryCandidates is read, not edited (task-1 diff on that file touches only the sink at :113-175). grader-r2.json C3: test passed, 0 'probe skipped' lines."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C4 APP CLASS = 'SIGNED THE WAY THIS PROCESS IS SIGNED'",
      "status": "pass",
      "evidence": "TrustedApplicationGate.swift:148 'public static func verify(_ path: String, appBundlePath: String, selfCode: SecStaticCode?) -> Bool'; :116-129 appClassOutcome implements the sequence (i) 'guard let selfCode else { return .noSelf }', (ii) :119-122 SecStaticCodeCheckValidity(selfCode, validationFlags, requirement(from: selfCertificatePrecondition)) == errSecSuccess else .selfFailsPrecondition, (iii) :124-127 SecCodeCopyDesignatedRequirement == errSecSuccess else .noDesignatedRequirement, (iv) :128 candidateSatisfies(candidatePath, requirement: selfRequirement). Tests :334-358 c4And5AppClassIdentitySignedCases: S1/S2 identity-signed '-i com.juzser.ClaudeStatusBar', S3 '-i com.other', S4 '--sign -' with the SAME identifier (:340); (4a) :349 verify(s2,s2,s1Code)==true, (4b) :352 verify(s4,s4,s1Code)==false, (4c) :355 verify(s3,s3,s1Code)==false, (4d) :358 verify(s4,s4,s4Code)==false. (4e) :381-393 c4eMakeAppScriptSetsNoCustomDesignatedRequirement resolves ../scripts/make-app.sh from #filePath, tokenises each codesign line on whitespace and asserts no '-r'/'--requirements' token with the pinned failure text; scripts/make-app.sh:102 and :104 still pass only '--force --options runtime --timestamp --sign' and are unchanged in the epic diff. grader-r2.json C4: both tests passed."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C5 NEITHER CLASS, AND NOTHING LOOSENED",
      "status": "pass",
      "evidence": "(5a) TrustedApplicationGateTests.swift:360-367 S2 relocated to a non-'claude' basename, verify(renamed, appBundlePath: s1, selfCode: s1Code) == false. (5b) :370-378 c5bUnsignedDirectoryAppClassWithNilSelfIsRejected: verify(appDir, appDir, selfCode: nil) == false. (5c) :76 defaultVerifyRejectsAdHocSignedCode and :232 defaultVerifyRejectsUnsignedDirectory survive; the only removed #expect lines in the diff are the three C1/C5d/C5e replacements. (5d) :212-217 defaultVerifySanityApproveClaudeClassRejectPlainFile: comment 'C5(5d): /bin/ls is now in NEITHER class', '#expect(TrustedApplicationGate.defaultVerify(\"/bin/ls\") == false)', plain-file rejection kept at :227. (5e) old defaultVerifyAcceptsSelfSignedLocalIdentity removed; :143 (2a) is its inverse. (5f) `grep -c @Test`: base 36 (git show 69f7613), after 52; 3 pre-existing @Test removed (the three named replacements), so 52 >= 36 - 1 + 9 tests C1..C4 add; suite summary in round2-swift-test.txt 'Test run with 434 tests passed' (grader-r2.json C5 cites the same)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C6 THE GATE STAYS SINGLE-FILE AND SUBPROCESS-FREE",
      "status": "pass",
      "evidence": "(6a-i) TrustedApplicationGateTests.swift:794-808 defaultVerifyClosureStaysInFile resolves the real gate file via #filePath exactly as the :775 probe does, calls DefaultVerifySpawnCheck.check(text, label: \"TrustedApplicationGate.swift\") and asserts verdict == nil with Comment(rawValue: verdict ?? \"\"). (6a-ii) :800-801 regex '(?:static\\s+)?func\\s+defaultVerify\\s*\\(' with comment 'defaultVerify is not declared as a func in TrustedApplicationGate.swift'; :802-804 reached.isSuperset(of: [\"defaultOutcome\",\"copySelfStaticCode\"]) with 'closure from defaultVerify does not reach: ...'; :1357 'static func reached(_ sourceText: String) -> Set<String>'. (6a-iii) :812 spawnCheckWalkRules with alpha (:820 acquireSelf() admitted), gamma (:841 expects 'defaultVerify -> defaultOutcome -> copySelfStaticCode'), beta (:852 expects 'defaultVerify -> p.spawnHelper'; :858 Self.helper() declared in-text yields nil); knownFrameworkSymbols is one Set at :1313; the plan's allowlist loop run on the checkout over its 10 names printed no 'ALLOWLIST NAMES REPO SYMBOL' line. (6a-iv) grep -nE 'Process|NSTask|posix_spawn|exec\\(' and '\"/(usr|bin|sbin|Applications)' on the gate file: no hits; grep -rlE 'func (defaultOutcome|copySelfStaticCode|classify|verify)\\b' Sources/ prints exactly Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift; fixture tests G+6/G-4/G-8/G-9/G-10 have no changed lines in the diff; :775 defaultVerifySourceContainsNoSpawnTokens kept. (6b) probes (g) :677-689 and (b) :662-668 have no changed lines in the diff. (6c) gate :220 onReject(path, \"per-path-class policy rejected: \\(className)\") with className 'app'/'claude'/'no path class'; test :904-909 filterRejectionReasonNamesPerPathClassNotAPolicyString (grader-r2.json C6). All cited tests passed in round2-swift-test.txt."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C7 THE SINK NEVER CREATES AN EMPTY ACL",
      "status": "pass",
      "evidence": "Filter unchanged in behaviour: gate :208 'guard !paths.isEmpty else { return [] }', :225 'return approved.isEmpty ? nil : approved' (base :76/:85 identical). LiveCredentialWriter.swift:113 additive default 'makeTrustedApplication: (String) -> SecTrustedApplication? = TrustedApplicationGate.defaultMakeTrustedApplication' (gate :185); :133-152: (i) 'var access: SecAccess?' set only 'if !filteredPaths.isEmpty', (ii) :140-149 'guard !trustedApps.isEmpty else { for path in filteredPaths { onReject(path, \"could not create a trusted application from an approved path\") }; return false }', (iii) :152 SecAccessCreate only with the non-empty array; :165-167 'if let access { newAttributes[kSecAttrAccess as String] = access }'. AccountCredentialVault.swift:189, :208-224, :232-234 mirror it. Tests: LiveCredentialWriterTests.swift:340 performWriteWithEmptyTrustedPathsOmitsAccessKey (:354 capturedUpdateAttributes?[kSecAttrAccess] == nil), :360 performWriteRefusesWhenApprovedPathsYieldNoTrustedApplication (:376-377 updateCalls == 0, addCalls == 0); AccountCredentialVaultTests.swift:316 and :332 the same pair (:326). 'trustedPaths: []' sites now 8 + 6 = 14 (12 pre-existing + one 7a per sink), none edited. grader-r2.json C7: all four tests passed. (7c) letter miss: the gate's filter doc comment (:191-198, base :63-70) was not rewritten to state (i)-(iii); the sinks' inline comments state them and the comment's 'system-default-ACL' sentence is now true because the sinks honour it — recorded in gaps."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C8 THE COMPOSED REGRESSION",
      "status": "pass",
      "evidence": "LiveCredentialWriterTests.swift:388 performWriteAllRejectedWritesExactlyOneDiagnosticLine: :401 'onReject: TrustedApplicationGate.rejectionHandler(writingTo: logURL)', :407-408 updateCalls == 0 and addCalls == 0, :413 'lines[0].components(separatedBy: \"\\t\").count == 3'. AccountCredentialVaultTests.swift:353 performRepairWriteAllRejectedWritesExactlyOneDiagnosticLine: :364 rejectionHandler(writingTo: logURL), :376 three tab fields. Gate :268 'public static func rejectionHandler(writingTo url: URL)' is the factory used; defaultOnReject (:277) is not referenced by either test. grader-r2.json C8: both passed and 'ls -la $CLAUDE_STATUS_BAR_HOME after the run shows only . and ..'; coder artifacts round2-apppaths-manifest-before/after.txt both empty (N8)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C9 PACKAGE.SWIFT EXCLUDES THE FIXTURES",
      "status": "pass",
      "evidence": "Package.swift:31 'exclude: [\"Fixtures\"],' inside .testTarget StatusBarCoreTests; the epic diff on Package.swift is exactly 1 insertion. `grep -n \"Fixtures/static-assertions\" Tests/StatusBarCoreTests/TrustedApplicationGateTests.swift` -> :735 (doc), :739 'testsDir.appendingPathComponent(\"Fixtures/static-assertions\")', :1528 '.appendingPathComponent(\"Fixtures/static-assertions\")' — still read by path. `ls Tests/StatusBarCoreTests/Fixtures/static-assertions | wc -l` = 16. Finding ...-0e5a7a62 fix-verified (ledger #118). Build/fixture-test evidence: grader-r2.json C9 (build exit 0, fixture tests among the 434 passing)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C10 THE LIVING DOCUMENT",
      "status": "pass",
      "evidence": "git diff 69f7613..2493d04 -- CLAUDE.md: the old sentence 'a code-signature check, not a location check, so a resolved path with no valid signature is dropped' is removed; the replacement states 'assigns each candidate path to one of two classes ... a path equal to this app's own bundle path is the `.app` class; a path whose last component is `claude` is the `.claude` class; anything else is in neither class and is dropped', 'The `.claude` class requires Anthropic's Developer ID (team `Q6L2SF6YDW`) and bundle identifier `com.anthropic.claude-code`, so a locally-minted certificate cannot be admitted', 'The `.app` class requires the candidate to be \"signed the way this process is signed\" ... computed at runtime rather than pinned as a hash in source — and admits nothing when the running process itself is ad-hoc or unsigned; under `swift run` that means the vault ACL repair is skipped rather than downgraded'. The diff touches only that paragraph (26 lines: 8 removed, 18 added); the Workflow section is unchanged."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C11 DEFAULTVERIFY IS WIRED END-TO-END FOR THE APP CLASS",
      "status": "pass",
      "evidence": "TrustedApplicationGate.swift:68-74 'internal enum VerifyOutcome { case noSelf, selfFailsPrecondition, noDesignatedRequirement, candidateRejected, approved }'; :158-161 'internal static func defaultOutcome(_ path: String, appBundlePath: String = Bundle.main.bundlePath, acquireSelf: () -> SecStaticCode? = copySelfStaticCode)'; :178-179 'public static func defaultVerify(_ path: String) -> Bool { defaultOutcome(path) == .approved }'; self acquired inside via acquireSelf() at :167. Test :448 c11p1DefaultOutcomeOnSelfMatchesMeasurement: measureSelf() runs codesign ['-dv'] and ['-d','-r-'] on the SecCodeCopyPath path (:422-424), prints the measurement, and branches 'P1 branch: approved' -> #expect(outcome == .approved) / 'P1 branch: selfFailsPrecondition' -> #expect(outcome == .selfFailsPrecondition) (:453-457). grader-r2.json C11: passed. Instrument deviation: hasCertificate is measured with SecStaticCodeCheckValidity against selfCertificatePrecondition (:437-441, with a comment on why the 'Authority=' text heuristic is unreliable) rather than the 'Authority=' grep the criterion names, and no 'codesign -v -R=<measured DR>' confirmation runs on the approved branch — recorded in gaps."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C12 THE SELF/CANDIDATE SWAP",
      "status": "pass",
      "evidence": "TrustedApplicationGateTests.swift:465 c12p2SelfCandidateSwapCaughtByAdHocSpecimen: ad-hoc S4 via appClassSpecimen(sign: \"-\", ...), skip line :468 'probe skipped: self is ad-hoc, swap unobservable', :474 '#expect(outcome == .candidateRejected)' on defaultOutcome(S4, appBundlePath: S4) with real self-acquisition (no selfCode injected). grader-r2.json C12: passed with 0 'probe skipped' lines (self carried a certificate in that run)."
    },
    {
      "task_id": "csb-signing-policy-1/task-1-per-path-class-signing-policy",
      "criterion": "C13 THE ACQUISITION-ERROR PATH AND THE BOOL REDUCTION",
      "status": "pass",
      "evidence": "TrustedApplicationGate.swift:79-85 'internal static func copySelfStaticCode() -> SecStaticCode?' guards 'selfStatus == errSecSuccess, let selfCode' and 'staticStatus == errSecSuccess, let staticCode', returning nil otherwise; no force-unwrap. Tests: :245-248 c13p3NilSelfYieldsNoSelf injects acquireSelf: { nil } and expects .noSelf; :253-266 c13p3AcquisitionCallsAreGuardedNotForceUnwrapped with the pinned text 'SecCodeCopyStaticCode result used without an errSecSuccess guard at TrustedApplicationGate.swift:<line>'; :276-298 c13p4DefaultVerifyIsExactlyTheOutcomeReduction over ['/bin/ls', selfPath, S4] asserting defaultVerify(p) == (defaultOutcome(p, appBundlePath: Bundle.main.bundlePath) == .approved). grader-r2.json C13: all three passed."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C1 THE HARNESS NEVER DELETES THE OPERATOR'S BUILD",
      "status": "pass",
      "evidence": "Static, measured on 2493d04: `grep -nE 'rm|find' scripts/signing-argv-test.sh` non-comment hits are the find-certificate at :26, 'rm -rf \"$FDIR\"' (11 sites), :395 'rm -f /tmp/csb-e-out.$$ /tmp/csb-e-rc.$$' and :403 'rm -rf \"$SCAFFOLD\"'; no argv names dist. `grep -n dist` in the harness returns only comments :383,:385 and the scaffold assertion :400 '[[ ! -e \"$SCAFFOLD/dist\" ]]'. Base :356 'if [[ -e dist ]]; then find dist -depth -exec rm -r -f {} + 2>/dev/null; fi' is gone. Behavioural sentinel run: not repeated here (make signing-test is prohibited for this dispatch); state/results/task-2-signing-harness-keeps-dist-and-escapes-argv.grader-r1.json C1 records SENTINEL-SURVIVED and 'signing-argv-test: all checks passed'."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C2 THE (e) REFUSAL CHECK STILL GRADES WHAT IT GRADED",
      "status": "pass",
      "evidence": "scripts/signing-argv-test.sh:383-386 comment '# ff55736f: never touch the repository's own dist/ here -- launch a copy of make-app.sh from a mktemp scaffold instead, since make-app.sh:20 cd's to its own parent'; :387-389 SCAFFOLD=\"$(mktemp -d)\", mkdir -p \"$SCAFFOLD/scripts\", cp \"$APPFILE\" \"$SCAFFOLD/scripts/make-app.sh\"; :391 '( \"${CLEAN[@]}\" CSB_SIGNING_DRY_RUN=1 /bin/bash \"$SCAFFOLD/scripts/make-app.sh\" ...' with the same 'sleep 5; kill -TERM' watchdog (:392); :400 assertion '[[ \"$E5RC\" -ne 0 ]] && has \"make-app: refusing to build a real artifact with CSB_SIGNING_DRY_RUN set\" \"$E5OUT\" && ! has 'Compiling' \"$E5OUT\" && [[ ! -e \"$SCAFFOLD/dist\" ]]' — the first three conjuncts byte-identical to base :367; :401 check description unchanged from base :368; :403 scaffold removed. grader-r1.json C2: the harness printed 'ok: (e) behavioural (clean launch, watchdog): make-app refuses to build with CSB_SIGNING_DRY_RUN set'."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C3 THE DRY-RUN PRINTER ESCAPES",
      "status": "pass",
      "evidence": "scripts/lib/signing-run.sh:18 'printf \"csb-dry-run-argv: %q\\n\" \"$token\"'; header :8-12 now reads 'prints argv one token per line, each line prefixed literally \"csb-dry-run-argv: \", %q-quoted so a token containing a newline ... still prints as exactly one prefixed line'; :23 '\"$@\"' execute path unchanged. Direct read-only measurement on the checkout: bash -c 'source scripts/lib/signing-run.sh; CSB_SIGNING_DRY_RUN=1 csb_signing_run /bin/echo \"$(printf \"a\\nb\")\"' printed 'csb-dry-run-argv: /bin/echo' and \"csb-dry-run-argv: $'a\\nb'\" — 2 lines, 0 unprefixed, no bare 'b'. Harness :302-304: the (d) check with exactly the plan's assertion line and bare description 'check \"(d) newline token is printed as one escaped line\" $?'; checks (a)-(c) at :290-300 unchanged. grader-r1.json C3: 'ok: (d) newline token is printed as one escaped line'."
    },
    {
      "task_id": "csb-signing-policy-1/task-2-signing-harness-keeps-dist-and-escapes-argv",
      "criterion": "C4 NOTHING ELSE MOVES",
      "status": "pass",
      "evidence": "`git diff --name-only 7d530e5..2493d04` (task-2's merge range) lists exactly scripts/lib/signing-run.sh and scripts/signing-argv-test.sh; `git diff --numstat` 5+4 and 37+3 = 49 changed lines (<= 200); scripts/make-app.sh and scripts/ensure-signing-identity.sh have no diff in the range. Non-comment 'security' invocations in the harness: only :26 'security find-certificate -c \"ClaudeStatusBar Local Signing\"' (:35/:130 are regex strings, :113 a message). '\"${CLEAN[@]}\"' launches: exactly :226 (B1) and :391 ((e)). Harness tail :459-461 still ends with 'signing-argv-test: all checks passed' on FAILURES == 0. grader-r1.json C4: make signing-test tail '---\\nsigning-argv-test: all checks passed'."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-1: The successor epic `csb-audit-1` named when it closed (ledger `csb-audit-1-2026-09-07#338`, `successor_epic`), carrying the one S2 that epic left standing.",
      "status": "pass",
      "evidence": "The S2 (a locally-minted Developer-ID-style certificate admitted as a trusted application for the credential item) is closed on 2493d04: TrustedApplicationGate.swift:35-36 pins the OU-bearing claudeRequirement and TrustedApplicationGateTests.swift:143-153 rejects the exact S2 specimen (local identity + '-i com.anthropic.claude-code'); grader-r2.json C2 records the probe ran (identity present) and passed. Integration branch smith/csb-signing-policy-1/integration at 2493d04 = merge(6bebbaf task-2) <- 7d530e5 = merge(f7a3cc3 task-1) <- 69f7613 (csb-audit-1 integration tip)."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-2: `csb-audit-1` task-2d added `TrustedApplicationGate` in front of the Keychain ACL grant, but its single `policyRequirement` string admits any Developer-ID-signed binary on the machine as a trusted application for the credential item — far weaker than \"the `claude` binary or this app\", which is what the grant is for.",
      "status": "pass",
      "evidence": "The single string is gone: `grep -n policyRequirement Sources/StatusBarCore/Accounts/TrustedApplicationGate.swift` has no hits (base :38 declared it). Its replacement is per class (:57-61 classify; :131-138 outcome switch: .claude -> claudeRequirement, .app -> appClassOutcome, nil -> .candidateRejected). 'certificate leaf[subject.CN] exists' survives only as selfCertificatePrecondition (:44), applied to SELF at :119-122, never to a candidate."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-3: The operator confirmed the finding and chose to land the gate as it was (better than no gate: it still refuses ad-hoc and unsigned binaries) and to fix the policy here rather than amend that plan a seventh time, because criteria 6 and 12 of task-2d had just pinned the requirement string and a new one needs its own specimens and its own grading.",
      "status": "pass",
      "evidence": "The new policy has its own specimens and grading on 2493d04: claude-class specimens at TrustedApplicationGateTests.swift:143-176 (local-identity, ad-hoc, Apple-signed-wrong-identifier), app-class S1-S4 at :334-340, real-binary probe :183-206; the old pinned-literal test was replaced by :36 (claudeRequirement) and selfCertificatePreconditionIsExactlyPinnedLiteral. The ad-hoc/unsigned refusals the operator relied on are retained (:76 defaultVerifyRejectsAdHocSignedCode, :232 defaultVerifyRejectsUnsignedDirectory; no removed #expect lines beyond the three replacements). Grading: grader-r1 fail (C6a) -> grader-r2 pass, 434 tests."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-4: Measured design direction: per-path-class policy — for the `claude` binary, `anchor apple generic and identifier \"com.anthropic.claude-code\"` plus `certificate leaf[subject.OU] = \"Q6L2SF6YDW\"`; for the app's own self-signed bundle, the running process's own designated requirement (a pinned leaf-certificate hash is the fallback if a self-signed DR does not validate without a trusted anchor); a path in neither class is refused.",
      "status": "pass",
      "evidence": "Clause met as written: claudeRequirement at TrustedApplicationGate.swift:35-36 is exactly the clause's string (csreq -r - -t exits 0; the real ~/.local/bin/claude reports Identifier=com.anthropic.claude-code, TeamIdentifier=Q6L2SF6YDW and its own DR contains 'certificate leaf[subject.OU] = Q6L2SF6YDW', so the pin is satisfiable by the shipped binary — grader-r2.json C3 test passed with no skip). App class uses SecCodeCopyDesignatedRequirement of self at :124-128 with no hash in source (grep for 'H\"' / 'cdhash' in the gate file: none); the primary DR path validated on this host (grader-r2 C4 (4a) passed with identity-signed S1/S2), so the hash fallback was not needed. Neither class -> .candidateRejected (:137-138; test :46 and :217). The security-reviewer's spec-change candidate (pin the Developer ID intermediate 'certificate 1[field.1.2.840.113635.100.6.2.6]') is outside the clause as written and is carried in gaps at 0.5, not as a fail."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-5: The same epic closes the empty-ACL hole at both `SecAccessCreate` sites (a rejected write must leave the old ACL alone, never write an item that names zero trusted applications) and fixes the signing test harness so it never deletes the operator's `dist/` and escapes what it prints.",
      "status": "pass",
      "evidence": "Both SecAccessCreate sites guarded: LiveCredentialWriter.swift:138-152 and AccountCredentialVault.swift:212-224 call SecAccessCreate only inside 'if !filteredPaths.isEmpty' after 'guard !trustedApps.isEmpty else { ...; return false }'; all-rejected filter (nil) returns false before any update/add at LiveCredentialWriter.swift:124-131 and AccountCredentialVault.swift:199-206. Finding ...-28a322bb fix-verified (ledger #114). Tests at both sinks: LiveCredentialWriterTests.swift:340/:360/:388, AccountCredentialVaultTests.swift:316/:332/:353. Harness: base :356 dist deletion removed, scaffold copy at scripts/signing-argv-test.sh:387-403; printer scripts/lib/signing-run.sh:18 printf %q, measured directly (2 prefixed lines for a newline token)."
    },
    {
      "task_id": "epic",
      "criterion": "goal-clause-6: Scoped like its parent: no UI, no `AppState`, no self-heal or locator files, and every agent bound by the same prohibition on mutating `/usr/bin/security` subcommands — the blast radius is the operator's real login keychain.",
      "status": "pass",
      "evidence": "`git diff --name-only 69f7613..2493d04` lists exactly 10 files: CLAUDE.md, Package.swift, the three Accounts/ sources, the three StatusBarCoreTests files, scripts/lib/signing-run.sh, scripts/signing-argv-test.sh — no AppState, no LiveCredentialSelfHeal, no locator, no UI path. Added diff lines grep for security import|set-key-partition-list|create-keychain|delete-keychain|add-generic-password|unlock-keychain|default-keychain|delete-certificate: none. The only /usr/bin/security launch in the tests is TrustedApplicationGateTests.swift:101-104 find-certificate (read-only); the harness's only security invocation is :26 find-certificate. Reviewer and security-reviewer result files for both tasks are empty finding lists ('[]'). This dispatch itself ran only read-only find-certificate, codesign -d/-dv/-d -r-, csreq -r - -t, git, grep, sed and one dry-run source of signing-run.sh."
    }
  ],
  "overall": "pass",
  "gaps": [
    {
      "criterion_ref": "task-1 C7 (7c)",
      "description": "The gate's filter doc comment (TrustedApplicationGate.swift:191-198, base :63-70) was not rewritten to state the sink policy (i)-(iii); only 'appBundlePath is used only to name the rejected path's class' was appended. The (i)-(iii) statements live as inline comments at LiveCredentialWriter.swift:133-151 and AccountCredentialVault.swift:208-223, and the gate comment's 'An empty input returns [], preserving today's system-default-ACL behaviour' is now true because the sinks omit kSecAttrAccess on an empty list. Letter miss, behaviour and tests met.",
      "confidence": 0.3,
      "suggested_task": null
    },
    {
      "criterion_ref": "task-1 C2 (2a), C3, C12 skip paths",
      "description": "The three identity/binary-gated probes print their pinned 'probe skipped: ...' line and `return` (TrustedApplicationGateTests.swift:145, :206, :468) instead of routing through `withKnownIssue`, so on a CI host the skip shows in stdout but the test is recorded as a pass, not a known issue. On this host all probes ran (0 'probe skipped' lines in round2-swift-test.txt; find-certificate exits 0; ~/.local/bin/claude is Developer-ID-signed), so no grading was lost here.",
      "confidence": 0.4,
      "suggested_task": "Wrap the three 'probe skipped' branches in withKnownIssue so a skipped probe is recorded as a known issue in the swift-testing summary rather than as a pass."
    },
    {
      "criterion_ref": "task-1 C11 (P1 instrument)",
      "description": "selfHasCertificate is derived from SecStaticCodeCheckValidity against selfCertificatePrecondition (TrustedApplicationGateTests.swift:437-441, with a comment that the 'Authority=' text heuristic misreads a CommandLineTools helper) rather than the 'Authority=' grep the criterion names, and the approved branch does not run the 'codesign -v -R=<measured DR>' confirmation. The measurement is stronger than the letter (it tests the exact predicate step (ii) applies) but is not independent of the Security-framework call the gate uses; the codesign -dv and -d -r- lines are still recorded.",
      "confidence": 0.3,
      "suggested_task": null
    },
    {
      "criterion_ref": "goal-clause-4 (spirit; security-reviewer spec-change candidate)",
      "description": "claudeRequirement pins 'anchor apple generic', the identifier and leaf[subject.OU] but not the Developer ID intermediate ('certificate 1[field.1.2.840.113635.100.6.2.6]') that Apple's own DR for the shipped binary carries (measured: ~/.local/bin/claude DR includes it). Without it, any Apple-anchored certificate issued to team Q6L2SF6YDW (e.g. an Apple Development or Mac App Store certificate of that team) also satisfies the claude class. The team pin already excludes every non-Anthropic signer, so the clause as written is met; the intermediate pin is a hardening that needs its own csreq validation, specimen and grading round, not a defect in this epic.",
      "confidence": 0.5,
      "suggested_task": "Extend claudeRequirement with 'and certificate 1[field.1.2.840.113635.100.6.2.6]' (and optionally leaf[field.1.2.840.113635.100.6.1.13]), re-validate with csreq, and re-grade C1/C3 against the real binary plus a negative specimen."
    },
    {
      "criterion_ref": "task-1 plan constraint: diff cap",
      "description": "task-1 landed 985 insertions + 74 deletions = 1059 changed lines against the plan's 900-line cap (git diff --stat 69f7613..7d530e5); the overrun was recorded and the gate passed at ledger #108. No code gap follows — the overrun is test-side (TrustedApplicationGateTests.swift +586) — but it is a planning lesson: C6 test-side closure work and C11-C13 wiring probes were enough for a sibling task.",
      "confidence": 1.0,
      "suggested_task": null
    },
    {
      "criterion_ref": "task-2 C3 (cosmetic)",
      "description": "The new harness check reuses the letter '(d)' (scripts/signing-argv-test.sh:304) already used by the pre-existing '(d) no-leak' check at :351 in another section; the plan pinned the description text, so the coder left it. Two 'ok: (d) ...' lines now print per run. Cosmetic.",
      "confidence": 0.3,
      "suggested_task": null
    },
    {
      "criterion_ref": "verdict brief record",
      "description": "The verdict brief named the base as b3934a5; on the checkout b3934a5 is task-2's branch tip (second commit on 6bebbaf's branch) and the true base of both merges is 69f7613 (7d530e5^1 = 2493d04^1). This verdict graded 69f7613..2493d04. Record correction only; no task.",
      "confidence": 1.0,
      "suggested_task": null
    }
  ],
  "next_plan_version": null
}
